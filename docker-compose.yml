version: '3.8'

services:
  onetagger:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./music:/app/music
      - ./config:/app/config
    environment:
      - RUST_LOG=info
    restart: unless-stopped
